spring:
  cloud:
    config:
      uri: http://172.10.40.174:30008
      name: counter-service
management:
  endpoints:
    web:
      exposure:
        include:
          refresh, health, beans, busrefresh